openpyxl>=3.1     # .xlsx BOM parsing
PyMuPDF>=1.24     # render schematic PDF pages to images (imported as `fitz`; pip-only, no poppler)
openai>=1.40      # vision model calls for schematic PDFs
Markdown>=3.5     # convert extracted markdown bodies to HTML (tables extension)
